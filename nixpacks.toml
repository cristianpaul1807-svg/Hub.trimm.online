[phases.setup]
nixPkgs = ["nodejs_18", "pnpm"]

[phases.install]
cmds = ["pnpm install"]
cacheDirectories = ["node_modules"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "npx serve dist -s -l 3000"
